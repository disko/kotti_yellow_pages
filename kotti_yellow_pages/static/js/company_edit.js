// Generated by CoffeeScript 1.6.1
var CompanyEditCtrl;

CompanyEditCtrl = function($scope, $http, addressService, mapquest) {
  var initMap, pos, setLatLng, updateAddress;
  updateAddress = function(name, value) {
    return $scope.$apply(function() {
      return $scope.address[name] = value;
    });
  };
  $scope.location = new L.LatLng(51, 10);
  $scope.address = {
    street: $('input[name=street]').change(function() {
      return updateAddress("street", this.value);
    }).val(),
    zipcode: $('input[name=zipcode]').change(function() {
      return updateAddress("zipcode", this.value);
    }).val(),
    city: $('input[name=city]').change(function() {
      return updateAddress("city", this.value);
    }).val(),
    country: $('input[name=country]').change(function() {
      return updateAddress("country", this.value);
    }).val()
  };
  $scope.setLocationFromAddress = function() {
    addressService.latLngForAddress($scope.address).then(function(latLng) {
      if (latLng) {
        $scope.location = latLng;
        return $scope.setMarkerFromLocation();
      }
    });
    return false;
  };
  $scope.setMarkerFromLocation = function() {
    $scope.marker.setLatLng($scope.location);
    $scope.map.panTo($scope.location);
    $scope.map.setZoom(14);
    $("input[name=latitude]").val($scope.location.lat);
    return $("input[name=longitude]").val($scope.location.lng);
  };
  initMap = function() {
    $scope.map = L.map("map", {
      zoomControl: true
    }).setView($scope.location, 6);
    mapquest.tileLayer.addTo($scope.map);
    $scope.marker = L.marker($scope.location, {
      draggable: true
    }).addTo($scope.map);
    return $scope.marker.on("dragend", function(e) {
      $("input[name=latitude]").val(e.target._latlng.lat);
      return $("input[name=longitude]").val(e.target._latlng.lng);
    });
  };
  setLatLng = function(pos) {
    $scope.location.lat = pos.coords.latitude;
    $scope.location.lng = pos.coords.longitude;
    $("input[name=latitude]").val($scope.location.lat);
    $("input[name=longitude]").val($scope.location.lng);
    $scope.map.setView($scope.location, 14);
    return $scope.marker.setLatLng($scope.location);
  };
  initMap();
  if (!($("input[name=latitude]").val() && $("input[name=longitude]").val())) {
    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(setLatLng);
    }
  } else {
    pos = {
      coords: {
        latitude: $("input[name=latitude]").val(),
        longitude: $("input[name=longitude]").val()
      }
    };
    setLatLng(pos);
  }
  $scope.$watch('address', $scope.setLocationFromAddress, true);
  return $scope.$watch('location', $scope.marker.update, true);
};
